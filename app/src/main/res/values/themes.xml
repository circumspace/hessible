<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Android window theme; actual colors are driven by MaterialExpressiveTheme in Compose.
         windowBackground is a day/night color so the launch window doesn't flash white in dark mode. -->
    <style name="Theme.Contactstr" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
